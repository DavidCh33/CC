name: docker Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: run docker
      - run: docker run -t -v `pwd`:/app/test davidch33/cc