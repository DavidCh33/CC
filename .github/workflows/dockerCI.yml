name: docker Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: run docker
        run: docker build -t davidch33/cc .
      - run: docker run -t -v `pwd`:/app/test davidch33/cc